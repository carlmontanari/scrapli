# Zig

libscrapli the core zig library that really *is* scrapli, currently has the least amount of collateral about how to use it as it is expected that the vast majority of users will use the Python and Go wrappers. If you'd like to check out a few zig examples though, head [here](https://github.com/scrapli/libscrapli/tree/main/examples)
